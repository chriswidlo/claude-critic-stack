# Decision log

Session: `2026-04-26-upgrades-lab-design`.

Step 10 verdicts were architecture=rework, operations=rework, product=rework; replan; routed to step 8 (frame revision) — two of three lenses produced frame-level objections converging on "lab over-builds new structure when capability-overlay + capture-first frame would cut the locking-dimension count from 4 to 1–2." Loop 1 of 2.

Step 10 (loop 2) verdicts were architecture=approve, operations=approve, product=rework; two-loop cap reached per CLAUDE.md; escalating to synthesis with product-lens disagreement named. Product's five deltas (split scripts by day, fix self-supersedes frontmatter, resolve implicit meta-files, specify ratio baseline, default lint hook ON) are concrete enough to fold into the synthesis as post-critique adjustments rather than triggering a forbidden third loop. Loop 2 of 2.
