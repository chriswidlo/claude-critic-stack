# Scope map — path-discipline regime

## Existing primitives touched

| primitive | source (where it was named) | relationship | one-line rationale |
|-----------|-----------------------------|--------------|--------------------|
| Path-discipline rule, clauses 1, 3, 4, 5 (no absolute paths; outside-repo refs by prose; copy-with-consent; no quoting outside paths) at `CLAUDE.md`:7-16 | requirement.md; explore §7 (zero leak hits); plan §"What the rule actually says" | extend | privacy/consent clauses are load-bearing and uncontested; the new regime adds enforcement substrate but does not alter their text. |
| Path-discipline rule, clause 2 (repo-root-relative link form) at `CLAUDE.md`:11 | requirement.md; plan §"Three options" | replace | every option (A reword + tooling, B flip, C hybrid) replaces clause 2 — A replaces its *justification surface*, B replaces its *style mandate*, C replaces both. Even Option A is not preservation: a clause whose stated reason is corrected and whose enforcement is moved into a slash command is no longer the same primitive. |
| Stated reason (a) — privacy/no-leak | CLAUDE.md:16; plan §"What the rule actually protects" | extend | grep audit returned zero hits; the reason is empirically holding. The new regime adds nothing to it; it survives unchanged. |
| Stated reason (b) — portability across machines/forks | CLAUDE.md:16 | extend | derivative of (a); same evidence; survives unchanged. |
| Stated reason (c) — "clickable from any clone" | CLAUDE.md:16; requirement.md (factually wrong); plan §"What's actually wrong" | replace | reason (c) is factually false on its current wording. Every option on the table replaces it (A rewords to grep-stability; B makes it true by flipping clause 2; C replaces with GitHub-only clickability). No option preserves the current text. |
| Stated reason (d) — consent boundary | CLAUDE.md:16 | extend | tied to clauses 3-5 (outside-repo); uncontested; survives. |
| `.claude/commands/upgrade.md` (slash-command authoring entrypoint) | explore §3 (zero mentions of path style across 45 lines); plan §"The /upgrade slash command does not instruct" | extend | the command's existing job (capture upgrade entries) is unchanged; the new regime adds a path-discipline instruction paragraph. This is genuine extension — current behavior preserved, new constraint layered. |
| Agent `frame-challenger` (Write-capable, produces path refs) | explore §4 | extend | agent's role unchanged; gains a one-line path-discipline directive in its prompt. |
| Agent `scope-mapper` (Write-capable, produces path refs) | explore §4 | extend | same — directive added, role intact. |
| Agent `subagent-distiller` (Write-capable, produces path refs) | explore §4 | extend | same. |
| Longitudinal-record contract (commit `5108ed3`: session artifacts immutable except for privacy redaction) | requirement.md; plan §"Session artifacts" | extend | the new regime explicitly defers to this contract — session artifacts are out of scope for any style sweep. The contract is unchanged and gains a citation in the new rule's enforcement clause. |
| 9 file-relative violator files (active surfaces: 6 upgrade README.md + 1 punch-list + 2 session-artifacts) | explore §1; plan §"What the 9-vs-9 split reveals" | replace (active surfaces) / extend (session artifacts) | the 6 upgrade READMEs and the punch-list are *active surfaces* — their current link form is replaced by the chosen canonical form. The 2 session-artifact violators are preserved by the longitudinal-record contract (see Preserved primitives). Multi-relationship across the set is honest because the set splits cleanly along the contract boundary. |
| 9 repo-root-style files (CLAUDE.md, prompts/ultraplan-next-level.md, 1 upgrade README, 6 session-artifacts, 1 karpathy session-artifact) | plan §"What the 9-vs-9 split reveals" | extend (under A) / replace (under B/C) | under A these files become canonical; under B their link form is replaced. The orchestrator has not picked yet, so this row is conditional — the frame-challenger should note that the relationship label here *is* the option choice. |
| `plans/2026-04-27-repo-cleanup-punch-list.md` item 1 | plan §"Parent doc"; punch-list §1 | subsume | item 1 of the punch-list IS the question this session is answering. The session's output (whatever option is picked) absorbs item 1's open status — item 1 is closed by writing this session's synthesis back into `CLAUDE.md`. |
| `plans/2026-04-27-item-01-path-discipline-decision.md` (operator's three-option plan) | requirement.md | subsume | the plan is a pre-decision artifact whose job is to enumerate options. The session's recommendation absorbs it — once a synthesis names the chosen option, the plan's enumeration role is complete. |
| Pre-commit hook / git hooks infrastructure | `.claude/settings.json`; `bin/` | (does not exist) | empty cell — `.claude/settings.json` only carries Read/Glob/Grep/Bash allow-lists and `rm`/`git push` deny rules. No hook config. The new regime's enforcement substrate (slash-command paragraph + agent directive) is a *new primitive*, not a modification of an existing one. Listed under "Primitives the distillations did not name" below. |

## Deletion cost (for subsume/replace rows)

- **Clause 2 (repo-root-style mandate):** under Option A, the deleted artifact is the current text of clause 2 + reason (c) — replaced by reworded text plus a tooling clause. Callers: every author/agent who reads CLAUDE.md before writing paths (currently 0 of 10 agents per explore §4 + zero slash-commands per explore §3 — so the *prose* caller surface is small, but the *behavioral* caller surface is every future write). Data migrations: none (clause 2 is prose). Config surface: one paragraph in CLAUDE.md, one paragraph in `.claude/commands/upgrade.md`, three one-liners in agent prompts. Under Option B: same prose surface plus rewriting 7 active repo-root-style files (CLAUDE.md self-references coincide; karpathy session-artifact is locked). Under Option C: same plus 16+ files.
- **Reason (c):** deleted text is one clause of the four-reason list. Callers: every reader who relied on the false claim (none observed; the claim has been silently false since `5108ed3`). Data migrations: none. Config surface: one half-sentence in CLAUDE.md.
- **9 file-relative violator files (active 7):** under any option that picks a single style, these have their link forms replaced. Blast radius: `upgrades/no-brainer/2026-04-26-critics-get-write-tool/README.md` (~10 link sites), `upgrades/normal/2026-04-26-living-poweruser-knowledge-module/README.md`, `upgrades/normal/2026-04-27-canon-r-and-d-lab-management-expansion/README.md`, `upgrades/normal/2026-04-27-decision-registry-adl/README.md`, `upgrades/normal/2026-04-27-workflow-docs-scattered-and-stale/README.md`, `upgrades/profound/2026-04-27-r-and-d-lab-thesis/README.md`, `plans/2026-04-27-repo-cleanup-punch-list.md`. Per-file: ≤1 sweep edit; verify each link resolves. No data migrations. Config surface: zero.
- **`plans/2026-04-27-repo-cleanup-punch-list.md` item 1:** subsumed when this session's synthesis writes into CLAUDE.md and item 1 is marked done. Callers: items 2 and 3 of the punch-list (blocked on item 1). Blast radius: appending `✅ done <date>` to item 1's heading per the punch-list's own usage instructions; items 2 and 3 unblock.
- **`plans/2026-04-27-item-01-path-discipline-decision.md`:** subsumed by the session synthesis. Callers: the operator who reads it for the option enumeration. Blast radius: the file remains in `plans/` as a historical decision-prep doc; no deletion required.

## Requires decision (conflicts)

None at this layer. The primitive that *would* be a conflict — clause 2 vs. file-relative natural-grain authoring — is presented as an option choice (A vs. B), not as an unresolvable contradiction. The orchestrator's pending pick is the resolution; this is not the scope-mapper's call.

One latent conflict the frame-challenger should test: **the longitudinal-record contract vs. any future tooling that "fixes" historical artifacts.** The current regime defers to the contract, so no live conflict — but if the enforcement substrate is mechanical (a pre-commit hook), it must be scoped to active surfaces only or it will break the contract. Flagging for downstream attention, not as a blocker here.

## Preserved primitives with stated reason (non-default)

- **Stated reason (a) — privacy/no-leak:** preserved because the introducing commit `5108ed3` names privacy as the rule's primary motivation, and the explore §7 audit returned zero leak violations. The empirical evidence is that this clause is doing its job; replacing it has no upside.
- **Stated reason (d) — consent boundary:** preserved because it is the operator's stated design intent (commit `5108ed3` explicit framing) and there is no surfaced friction with current authoring practice.
- **Longitudinal-record contract (session-artifact immutability):** preserved because the operator stated the constraint explicitly in the requirement (*"session-artifact files must NOT be retroactively edited for style"*) and commit `5108ed3` carries it as a quoted rule. This is a user-stated preserve; the frame-challenger may still test whether a *future* mechanical-gate primitive would force a renegotiation, but the contract itself is not in scope for replacement.
- **2 file-relative session-artifact violators** (`.claude/session-artifacts/2026-04-27-critics-get-write-tool-impl/explore-raw.md`, `.claude/session-artifacts/2026-04-27-critics-get-write-tool-meta-review/meta-review.md`): preserved as direct consequence of the longitudinal-record contract above, not as an independent decision. The plan's correction-of-the-punch-list (step 5) makes this explicit.

## Primitives the distillations did not name but the query implies

- **A pre-commit / write-time enforcement gate (`bin/check-path-discipline.sh` or equivalent).** The plan's step 6 floats it as optional; the outside-view distillation's verdict says Option A is below base rate *unless* a non-optional mechanical gate exists; the canon-librarian distillation's poka-yoke + automate-the-procedure passages point the same direction. None of the three existing pre-commit, hook, or CI primitives exist on disk (`.claude/settings.json` carries no hook config; `bin/` was not surfaced in the distillations). If the synthesis recommends Option A, this is a new primitive to be created, not an existing one to be mapped.
- **An agent-side path-discipline validator** (a directive added to the three Write-capable agents and the slash-command, plus a check pass). Implied by explore §4 (zero of 10 agents mention the rule) and the canon-librarian's "agent in the loop" framing. Same status — new primitive, not mapped here.
- **A measurement primitive** (a periodic audit script that recomputes the 9-vs-9 split). Outside-view's clarifying experiment proposal. New, not existing.
- **`upgrades/LEDGER.md` and the upgrade-entry tier folders.** Not directly touched by the rule's text, but they are the *destination* for the file-relative violators. Their existence shapes the deletion cost above; they are not modified by this question.

A follow-up Explore is not required — the distillations are complete on existing primitives. The above three are *proposed new primitives*, which the generator step (not the scope-mapper) will weigh.
