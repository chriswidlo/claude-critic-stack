# Critic Panel Verdicts

| Lens | Verdict | Frame-level objection (verbatim summary) |
|------|---------|------------------------------------------|
| Architecture | rework | Frame E was carried in the verb, not in the boundary; Layer 5 is a different bounded context, not a layer; `Instrument` must be an aggregate root with ACL between Layer 1 taxonomy and Layer 4 tool names. |
| Operations | rework | This is a continuously-operating regulated data product, not a retrieval architecture. Load-bearing primitive is the ingest quarantine + replay + corrigenda state machine, not retrieval shape. Architecture is correct but inoperable at retail-Q&A cost envelope; indexing cost is paid once per corpus regardless of consumer. |
| Product | rework | The candidate steamrolled the user's stated mode. User said "research, do not execute or create anything" — candidate handed them a build plan. Synthesis deliverable should be a **field map** with the prescription demoted to an optional appendix. AU-specific specificity is thin; "APRA/ASIC/AFSL" sprinkled on a generic regulated-RAG blog post. |

## Aggregate

**Three rework, zero approve.** Minority veto fires unanimously.

## Convergence point across critics

All three independently identified that **the candidate's deliverable shape is wrong for the user's mode** — product names it directly; architecture and operations name it via "the prescription is presented with conviction it has not earned" (architecture: invariants asserted but not boundary-enforced; operations: SLAs named but operational states unmodeled).
