# Scope map — item 7 (residual AGENTS.md references)

## Existing primitives touched

| primitive | source (where it was named) | relationship | one-line rationale |
|-----------|-----------------------------|--------------|--------------------|
| Path-discipline rule artifact list in [CLAUDE.md:9](../../../CLAUDE.md) | Explore distillation, fact 3; reframe Job 1 | replace | Enumerative list naming `AGENTS.md` is broken (no referent); the rule intent survives if the list is rewritten to name the *class* of root-level constitutional docs or simply drops `AGENTS.md`. The string is replaced; the rule is preserved. |
| `.gitignore` allowlist line `!AGENTS.md` at [.gitignore:21](../../../.gitignore) | Explore distillation, fact 2; reframe Job 1 | replace | An allowlist entry for a nonexistent file is inert today and a forward-promise tomorrow. Default frame says: delete the line; observable behavior unchanged. The allowlist mechanism itself stays; only this entry goes. |
| "🚀 upgrade" benefit cell at [constitutional-layer-goals-modules/README.md:11](../../../upgrades/no-brainer/2026-04-26-constitutional-layer-goals-modules/README.md) | Explore distillation, fact 7; reframe Job 2 | replace | Benefit cell currently asserts parity between two docs, one of which does not exist — the *claim* is partially false. Replacing the cell text to match what is now true (CLAUDE.md drift detection only, or retiring the parity premise explicitly) restores claim integrity. Cell stays in place; its content is rewritten. |
| "🚀 upgrade" benefit cell at [git-strategy-documentation/README.md:11](../../../upgrades/no-brainer/2026-04-26-git-strategy-documentation/README.md) | Explore distillation, fact 9; reframe Job 2 | replace | Same shape as the row above: the parity premise has lost a leg; the cell must be rewritten to reflect commit-traceability of the artifacts that actually exist. Cell location preserved; content replaced. |
| Module-composition table row at [constitutional-layer-goals-modules/README.md:42](../../../upgrades/no-brainer/2026-04-26-constitutional-layer-goals-modules/README.md) | Explore distillation, fact 8; reframe Job 3 | replace | The "orchestration" module is enumerated as `{CLAUDE.md, AGENTS.md, the workflow, gates}`. The current shape of the module no longer includes `AGENTS.md`; leaving the row as-is documents a structure the repo does not have. The row stays; its membership list is replaced with the current composition. |
| Catalyst sections at [workflow-blind-to-the-lab/README.md:9](../../../upgrades/profound/2026-04-26-workflow-blind-to-the-lab/README.md) and [:38](../../../upgrades/profound/2026-04-26-workflow-blind-to-the-lab/README.md) | Explore distillation, facts 5–6; reframe Job 4 | extend | This is the one preservation-with-reason in the map. The catalyst is a dated diagnostic ("CLAUDE.md and AGENTS.md contained zero references to upgrades/ at time of investigation"). Editing the past tense is retconning. The honest move is to leave the prose verbatim and *extend* it with a forward-pointer footnote — "AGENTS.md has since been retired; see item 7" — so a reader is not misled into thinking AGENTS.md still exists. Reasoning detailed below. |
| Session-artifacts immutability convention ([.claude/session-artifacts/README.md](../../../.claude/session-artifacts/README.md)) | User prose; requirement.md user-framing line "Session-artifact mentions are historical record — don't edit" | extend | The convention already covers this case; no change to the convention itself. Item 7's resolution will *exercise* the convention by leaving session-artifact occurrences untouched. The convention is reinforced by being honored, not modified. |
| Punch-list "✅ done" marker pattern at [plans/2026-04-27-repo-cleanup-punch-list.md:259-263](../../../plans/2026-04-27-repo-cleanup-punch-list.md) | User prose ("punch-list parent"); requirement.md gap on stale line numbers | extend | The pattern itself is fine; item 7's completion writes one more `✅` row using it. Note the pre-existing line drift (punch-list says line 33 for `.gitignore`, current file is line 21) — that staleness is a *separate* problem not in scope here, but flagged for the generator. |
| Upgrade-entry lifecycle marker convention (🩺/🔨/🔖/✅ row in upgrade README headers) | User prose; pattern visible across `upgrades/no-brainer/*/README.md` | extend | Editing benefit cells in two upgrade entries that already carry lifecycle markers raises the question of whether the edit advances the marker (e.g., does revising a 🔖-marked entry require a new marker?). The convention is not modified; the generator must decide *how* to honor it for entries whose body is being edited post-finalization. Flag, not change. |

## Deletion cost (for subsume/replace rows)

- **`CLAUDE.md:9` path-discipline list**: callers = every artifact that quotes or paraphrases this rule (none surfaced in distillations); data migrations = none; config surface = the rule itself, one inline list of artifact names. Blast radius: textual only. The rule's *force* is preserved; only the enumeration changes.
- **`.gitignore:21` `!AGENTS.md`**: callers = git's ignore evaluation pass — currently inert because the file does not exist; data migrations = none; config surface = one line in `.gitignore`. Blast radius: zero today. Future cost: if AGENTS.md is ever recreated, the allowlist line would need re-adding. That cost is named and rejected — recreation is out of scope per the frame ("Restore AGENTS.md is out of scope").
- **`constitutional-layer-goals-modules/README.md:11` benefit cell**: callers = readers of the upgrade entry's value-proposition; data migrations = none; config surface = one cell of one row. Blast radius: the *argument* of the upgrade entry — replacing the cell forces a judgment on whether the original parity-based benefit claim still holds with one premise removed (outside-view distillation flags this as the silent-scope-shrink trap, Cat 2).
- **`git-strategy-documentation/README.md:11` benefit cell**: same shape; same blast radius. Generator must address whether commit-traceability of *one* doc was the original load-bearing claim, or whether the claim was specifically about *drift between two*.
- **`constitutional-layer-goals-modules/README.md:42` module row**: callers = anyone using the module decomposition as a reference for "what's in the orchestration module"; data migrations = none; config surface = one table row. Blast radius: the structural claim of the upgrade entry — if removing `AGENTS.md` from the row leaves the module looking thin, the generator must decide whether the module's composition argument still holds or whether the upgrade entry needs a deeper revision.

## Requires decision (conflicts)

None. No primitive in this map cannot coexist with the requirement. The "minimum-disruption hygiene" vs "prescription integrity" tension surfaced in the reframe is a *frame* tension (which the frame-challenger should test), not a primitive-level conflict — both frames agree on what to touch; they disagree on how deeply to rewrite the touched cells.

## Preserved primitives with stated reason (non-default)

- **Catalyst sections at [workflow-blind-to-the-lab/README.md:9](../../../upgrades/profound/2026-04-26-workflow-blind-to-the-lab/README.md) and [:38](../../../upgrades/profound/2026-04-26-workflow-blind-to-the-lab/README.md)**: preserved because (a) the user's explicit constraint in the requirement framing is "Catalyst sections describing the past can stay" and "Session-artifact mentions are historical record — don't edit"; the catalyst is a peer-class artifact (a dated investigation finding embedded in a finalized upgrade entry). (b) The named concrete cost of editing them is *retconning history under current knowledge* — the catalyst documents what was true at the time of the grep that motivated the upgrade; rewriting it would erase the diagnostic that justified the upgrade's existence, which is the load-bearing reason the upgrade was approved. (c) This is consistent with the canon-librarian's "leave-the-trail" pole (Google SRE 2016 postmortem culture, directly quotable: postmortems are kept "so that others can find and learn from"). The relationship is `extend` (add a forward-pointer footnote so a reader is not misled), not bare `preserve` — the surface remains; a small explicit signal is added that the named partner doc was later retired.

  **Counter-pressure named:** the outside-view distillation explicitly warns that *upgrade-entry cells are also dated records*, not just catalysts, and that retiring/marking benefit cells (the Cat 2 `replace` rows above) "itself contaminates the historical record of what the upgrade was originally claimed to do." If the frame-challenger accepts that warning, the symmetric move is `extend` for the benefit cells too (strikethrough + footnote rather than rewrite). The map records this tension; the generator must resolve it, not the scope-mapper.

- **Session-artifacts immutability convention**: preserved because the convention is the load-bearing reason this whole item is bounded ("don't edit session-artifacts"). Editing the convention here would be circular.

- **Punch-list ✅ pattern and upgrade-entry lifecycle markers**: preserved because they are *meta-conventions* the requirement uses to record its own completion, not artifacts the requirement targets. Removing or modifying them would be out of scope.

## Primitives the distillations did not name but the query implies

- **Git history of `AGENTS.md`** — Explore flagged the gap explicitly: was AGENTS.md ever tracked in git, or was it always aspirational? The answer changes the rhetorical force of the catalyst (if AGENTS.md never existed in tree, the catalyst's claim "CLAUDE.md and AGENTS.md contain zero references to upgrades/" is doubly damning, not retconned). One-line `git log --all -- AGENTS.md` would resolve.
- **Upstream/external AGENTS.md convention** — whether AGENTS.md is a standard Claude Code or agent-OS convention the user may eventually honor. If yes, the `.gitignore:21` allowlist line is a forward-compat hook, not dead weight; default-replace would be wrong. Explore flagged this as unverified.
- **The other places in the repo that quote or paraphrase the path-discipline rule** — Explore audited only `AGENTS.md` references, not "places that say 'CLAUDE.md, AGENTS.md, README.md' or any subset." If the path-discipline rule is restated elsewhere (agent prompts, workflow docs, prior upgrade entries), those restatements need the same treatment. A second Explore pass with a wider grep (`-l "CLAUDE\.md.*AGENTS\.md"` or similar) would close this gap before generation.
- **A "supersede, don't delete" governance norm** for upgrade entries — canon-librarian flagged ADR-style "Superseded-by" headers as a corpus gap but a well-known external pattern. If the user wants Cat 2 cells marked as "premise revised, see item 7" rather than rewritten, this primitive does not yet exist in the repo and would have to be invented. The frame-challenger should test whether inventing that primitive is in scope for item 7 or belongs in a separate item.
