# Critic-panel aggregate — ark-mono phantom fixture

## Loop 1 (candidate v1)

| lens | verdict | frame-level objection (one-liner) |
|------|---------|-----------------------------------|
| architecture | rework | Exemplar-contract introduced without a home module; regression-discipline subsystem doesn't cohere. |
| operations | rework | A contract authored by the same operator at the same attention budget that caused the deferral, with no forcing function, is a wish, not a control. |
| product | rework | Primary surface is CLAUDE.md, not the punch-list; introducing a placeholder there and deferring substance to a less-read artifact inverts surface hierarchy. |

Aggregate: rework → rewrite (not replan; objections were design-level sharpening of F4).

## Loop 2 (candidate v2)

| lens | verdict | frame-level objection (one-liner) |
|------|---------|-----------------------------------|
| architecture | **rework** | Subsystem still doesn't cohere — v2 authored a contract document describing operations that nothing performs; phantom moved from data to protocol. mtime is not a mechanism. Contract not co-located with governed directory. |
| operations | **approve** (defensible-but-weak) | mtime is "documentation-disguised-as-mechanism" but acceptable as v1 of the discipline for a single-operator stack with an explicit upgrade path (assumption 2's check script). |
| product | **approve** | CLAUDE.md is now real-slug; contract self-declares provisional status; punch-list shrinks net; surface hierarchy correct. |

## Two-loop cap reached

Per CLAUDE.md step 11: *"Cap at two full loops; past that, escalate to synthesis with the disagreements named."*

Loop 2 has one residual veto from architecture. Loop count is now 2. Escalation rule applies: synthesis must name the disagreement, not collapse it.

**Disagreement to surface in synthesis:**

> The architecture lens holds that the regression-discipline subsystem requires an *executable* mechanism (a check script, an agent that owns review, or a co-located contract) before any candidate can be approved. v2's mtime-gated review and out-of-directory contract are documentation, not execution. The operations and product lenses approve v2 as a defensible v1-of-the-discipline given the single-operator nature of the stack, with named upgrade paths. The synthesis ships v2 with the architecture objection recorded as the next-iteration question, not resolved.

See [decision-log.md](.claude/session-artifacts/2026-04-27-ark-mono-phantom-fixture/decision-log.md).
