# Decision log — ark-mono phantom fixture

## Step 10 → step 11 routing

Step 10 verdict was architecture = rework, operations = rework, product = rework; rewrite; routed to step 9.

Reason: all three lens objections are design-level (specific actionable issues with the candidate's structure: missing contract owner, missing forcing function, wrong product surface for the placeholder). The one frame-level objection (architecture's "regression-discipline subsystem doesn't exist as a coherent unit") is a sharpening of F4 from challenges.md, not a new frame. Replan threshold not met.

Loop count: 1 (cap is 2 per CLAUDE.md).

## Step 10 (loop 2) → step 12 escalation

Step 10 verdict was architecture = rework, operations = approve, product = approve; **two-loop cap reached; escalate to synthesis with disagreement named**.

Reason: architecture's residual rework is design-level (mtime is documentation-disguised-as-mechanism; contract not co-located with governed directory). It is a sharpened version of loop-1's "regression-discipline subsystem doesn't cohere as a unit." Operations and product approve v2 with explicit named caveats. Per CLAUDE.md: past two loops, synthesize with disagreement named — do not loop further.

Loop count: 2. Cap: reached. Escalation: invoked.
