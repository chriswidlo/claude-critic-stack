# Decision log — 2026-04-27-shadow-comparator-impl

## Step 11 routing

Step 10 verdict was: architecture = rework, operations = approve, product = rework. Both rework verdicts are design-level (comparator schema specifics, synthesis rendering convention). Routed to step 9 (rewrite, not replan). Adjustments folded into the implementation:

1. Comparator output uses a structured table schema, not free prose.
2. CLAUDE.md routing-rule text reframes the panel as "three lenses + optional triangulation meta-lens" rather than silently growing to seven agents.
3. Synthesis rendering for "Triangulation signal" is documented in the routing rule.

No frame-level veto. Single rewrite loop, well within the two-loop cap.
