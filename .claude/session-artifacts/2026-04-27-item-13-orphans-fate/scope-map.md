# scope-map.md — item 13 (orphans fate)

Two-layer map: per-orphan rows (O1–O4), then a separate row for the standing-policy layer (P).

## Existing primitives touched

| primitive | source (where it was named) | relationship | one-line rationale |
|-----------|-----------------------------|--------------|--------------------|
| O1 — [prompts/five-pressures.md](../../../prompts/five-pressures.md) | user prose; reframe sub-question 1 (reusable artifact) | replace | The 12-step workflow already encodes adversarial review (frame-challenger + critic-panel + outside-view) that this 37-line LLM-failure checklist was a precursor to; canon's "simplest solution / basic components" (Anthropic 2024) argues against keeping a parallel checklist whose responsibility the workflow now owns. |
| O2 — [prompts/ultraplan-next-level.md](../../../prompts/ultraplan-next-level.md) | user prose; reframe sub-question 1 (frozen evidence of meta-decision) | replace | Git history already preserves the brief and the punch-list it produced ([plans/2026-04-27-repo-cleanup-punch-list.md](../../../plans/2026-04-27-repo-cleanup-punch-list.md)); the brief's only live function (commission an external planning pass) has discharged. Git is the venue for frozen evidence; keeping a 26 KB prose artifact in `prompts/` claims the slot for "live, reusable instruction" that this no longer is. |
| O3 — [plans/ok-cool-this-is-warm-balloon.md](../../../plans/ok-cool-this-is-warm-balloon.md) | user prose; reframe sub-question 1 (frozen evidence of past decision) | replace | Phase-2 fully shipped per git log; `plans/` convention is *de facto* "in-flight punch-list items" (six other entries are dated 2026-04-27 and active); a shipped plan is a category mismatch. Git history (commit messages + the shipped code) is the venue. |
| O4 — [.claude/session-artifacts/exemplars/](../../exemplars/) | user prose; entangled with punch-list item 5 | conflict | The promotion-mechanism that would populate `exemplars/` is the subject of item 5 (ark-mono phantom fixture); deciding O4 here pre-empts that decision. Not mappable in this session — see Requires decision. |
| P — standing policy for "expired-utility documents" | user prose (two-layer ask, layer a) | subsume | The combination of (i) [upgrades/LEDGER.md](../../../upgrades/LEDGER.md) `state` lifecycle for *prospective* work, (ii) `.claude/session-artifacts/` immutability for *historical record*, (iii) git history for *frozen evidence*, and (iv) the absence of a `temporary/` folder (deliberately deleted, item 6) already covers the lifecycle space. A new "expired-utility" primitive would be the extra abstraction layer Anthropic 2024 warns against, applied to N=4 with no recurring cadence (outside-view: below base rate for option a). |

## Deletion cost (for subsume/replace rows)

- **O1 (five-pressures.md).** Callers: 1 internal reference (ultraplan-next-level itself, also being deleted as O2); 1 historical session-artifact reference (already immutable, link will gracefully 404 in the artifact, which is acceptable per session-artifact README). Data migrations: none — content is a static checklist; if any specific failure mode is not redundantly covered by the critic-panel lenses, that gap is the one thing worth porting (likely as a single bullet in [critic-architecture](../../../.claude/agents/critic-architecture.md) or the orchestrator's CLAUDE.md anti-patterns list). Config surface: none.
- **O2 (ultraplan-next-level.md).** Callers: zero load-bearing; one cross-reference from O1 (also being deleted). Data migrations: the brief's outputs already exist as the punch-list and the per-item plans — no content needs to migrate. Config surface: removes the `prompts/` folder's only ambiguous occupant; with O1 also gone, the folder convention either empties (delete folder) or needs a one-line `prompts/README.md` if any future file is anticipated. Blast radius: the 26 KB is recoverable via `git show <sha>:prompts/ultraplan-next-level.md` for any future reader who wants the meta-context.
- **O3 (ok-cool-this-is-warm-balloon.md).** Callers: none (reframe and requirement.md confirm "fully shipped per git log"; no live doc points at it). Data migrations: none — the plan's content is realized in shipped code; the rationale lives in commit messages. Config surface: leaves `plans/` containing only in-flight items, which is the convention's natural shape. Blast radius: a future "why did we build Phase-2 this way" question is answered by `git log -- plans/ok-cool-this-is-warm-balloon.md` and `git show`.
- **P (policy).** What gets "deleted" is the *unwritten* policy the requirement implies needs writing — the deletion cost is rhetorical, not artifact-bearing. Blast radius: the orchestrator must accept that the next sweep (sweep 2) will re-decide each orphan from scratch using the same primitives; the canon-librarian's contradiction (SRE "lasting improvement from one-time effort" vs. Anthropic "simplest solution") is unresolved and remains a live tension if a third sweep ever happens. Outside-view's mitigation (a one-line rule somewhere) is *not* the same as a new primitive — see Preserved primitives.

## Requires decision (conflicts)

- **O4 ([.claude/session-artifacts/exemplars/](../../exemplars/)) vs item 5 (ark-mono phantom fixture).** Both items reference the same primitive (the promotion mechanism that would populate exemplars). Item 5's session ([2026-04-27-ark-mono-phantom-fixture](../../2026-04-27-ark-mono-phantom-fixture/)) is the venue where the promotion-mechanism's fate is being decided. Deciding O4 here ("delete the empty folder") pre-empts the item-5 question of whether the mechanism should be repaired (in which case the folder stays) or abandoned (in which case the folder goes). User has not chosen which item owns this call. Recommend the generator either (a) defer O4 to item 5 and produce a 3-orphan answer here, or (b) flag O4's disposition as contingent on item 5's outcome. Do not silently dispose.

## Preserved primitives with stated reason (non-default)

- **Git history as the venue for frozen evidence.** Preserved (i.e., relied on rather than augmented) because the user's reframe explicitly identifies it: "the orphans are not cruft but frozen evidence — and the right primitive is not an archive folder but git itself." Concrete cost of replacing it with an `archive/` folder: maintained-index staleness (Anthropic 2025, hybrid-retrieval passage — maintained indexes go stale; glob/grep stays fresh). Git already has the property the policy would re-implement.
- **The deleted `temporary/` folder (gone per item 6).** Preserved-as-deleted; recreating it under a different name (`archive/`, `attic/`, `retired/`) would re-introduce the same primitive item 6 just removed. Concrete cost of recreating: contradicts an in-flight punch-list decision and creates two adjacent "stuff that isn't quite live" venues.
- **One-line rule in CLAUDE.md or [decision-log.md](../../decision-log.md).** Preserved as the *only* permitted policy-shaped artifact (per outside-view's mitigation: "Default for orphans is delete; archive only for historical value to a future decision; wire-in only if load-bearing" — ~30 seconds of work, no new primitive). This is `extend` of CLAUDE.md, not a new primitive — kept here as a flag for the generator, not as a recommendation. Concrete cost of *not* having this: outside-view's failure mode for ad-hoc — "inconsistency surfacing at second sweep; maintainer cannot reconstruct rule 6–12 months later." Cost is bounded (re-decision, not lost data).

## Primitives the distillations did not name but the query implies

- **`prompts/` folder convention itself.** Currently undocumented; holds only O1 and O2. If both are removed, the folder is empty — implicit primitive "we have a prompts folder for X" needs to be either deleted-with-folder or made explicit with a README. Neither distillation addressed this.
- **Cross-orphan reference graph.** O1 references O2; O2 is referenced only by O1. Removing both at once is cleaner than sequencing; removing one leaves a dangling reference in the other. Generator should treat O1+O2 as a unit, not two independent decisions.
- **Sweep cadence.** Outside-view assumed "first sweep" but flagged this as unverified; the session-artifacts directory shows multiple 2026-04-27 items, which may already constitute recurring cadence. If sweep 2 happens within (e.g.) 6 months, the base-rate calculus shifts toward option (a). The generator should name the sweep-cadence assumption explicitly and define what observation would flip it.
- **Canon-refresher targets.** Librarian flagged Matuschak (*evergreen notes*) and Hunt & Thomas (*Pragmatic Programmer*, "DRY for knowledge") as missing canon entries that bear on orphan-vs-reference distinction. Not in scope for this map; flagged for canon-refresher.
