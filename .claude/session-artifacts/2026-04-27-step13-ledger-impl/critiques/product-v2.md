# Critic — product lens, loop 2 on candidate-v2

## What changed
Citation-pattern-as-required-language in CLAUDE.md (✓). Threshold-tuning instructions inline (✓). Operations' pre-merge items absorbed; deferrals labeled.

## Frame-level posture
The "ship-the-minimum" concern is satisfied. v2 is at the right complexity: 8 pre-merge items, all silent-failure preventers or singularity-of-source enforcers. No premature optimization, no schema enrichment beyond what real sessions will demand.

## Residual concerns (non-blocking)
- The synthesis citation line ("Ledger: ...") is the load-bearing user-value piece. If real operators report the line is in the wrong format or hard to scan, the format is in one place (CLAUDE.md step 13 spec) and is cheap to revise.
- The 5-ledger-tuning recommendation is implicit in "starting heuristics." Worth restating in [.claude/session-artifacts/README.md](.claude/session-artifacts/README.md) ("after 5 ledgers, review distribution and adjust thresholds in this section") so the trigger is concrete.

## Verdict
**approve.**
