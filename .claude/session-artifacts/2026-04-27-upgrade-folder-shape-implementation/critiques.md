# Critic-panel verdicts

## Loop 1 (candidate v1 — script + LEDGER counter + slug-rep deferral)

| Lens | Verdict | One-line |
|---|---|---|
| architecture | `rework` | LEDGER silently promoted from index to dashboard; `bin/` permanent commitment treated as directory choice. |
| operations | `rework` | Forward-path engineering for recovery-shape problem; idempotency single-bit, misses half-state. |
| product | `rework` | Slug-vs-README.md deferred when reversal is 30× more expensive after migration; three open questions = orchestrator uncomfortable committing. |

**Convergent insight:** the candidate accretes structure (counter, script, deferred decisions) that looks like discipline but actually defers the load-bearing decisions.

**Routing:** rewrite candidate v2 with the convergent insight as the frame.

## Loop 2 (candidate v2 — strangler + README.md + no script)

| Lens | Verdict | One-line |
|---|---|---|
| architecture | `approve` | All three loop-1 flip-conditions cleared; gate-coupling and README.md collision are documentation tasks, not architectural faults. |
| operations | `approve` | Recovery-shape failure class dissolved by removing batch; mixed-shape cost lower than loop 1 credited; on-demand `find` is right shape. |
| product | `rework` | v2 silently revises what operator accepted (batch+`bin/` → strangler); orchestrator escalated wrong decision (slug-vs-README.md instead of strangler-vs-batch). |

## Aggregate after loop 2
Minority veto: product rework. Cap reached (2 loops per CLAUDE.md). **Escalate to synthesis with disagreement named.**

The product critic's objection is fair and the synthesis should adopt it: surface the strangler-vs-batch decision as the headline operator-facing question, not slug-vs-README.md. The implementation cannot proceed under v2's silent revision; operator must explicitly choose batch (PREPARE/v1 plan) or strangler (v2 plan).

Architecture and operations both approved v2's content; their approval stands conditional on the operator choosing strangler. If operator chooses batch, the loop-1 critiques re-apply and the script needs the lift conditions named there.
